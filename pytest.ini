[pytest]
# pytest-homeassistant-custom-component fixtures (hass, etc.) are async, and the
# test functions are async too; auto mode lets bare `pytest` collect them without
# per-test markers (required on pytest-asyncio >= 1.0).
asyncio_mode = auto
